sudo: false

language: node_js

node_js:
  - '0.10'
  - '0.12'
  - '4'
  - '6'
  - '7'

branches:
  only:
    - master
    - travis-ci

install:
  - npm install proof istanbul coveralls
  - for dir in mingle.*; do (cd $dir && npm install); done

script: t/test
